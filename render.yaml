services:
  - type: web
    name: secops-phishing-scanner-feature
    runtime: python
    plan: free
    branch: feature/scanner-ui-caution-updates
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
